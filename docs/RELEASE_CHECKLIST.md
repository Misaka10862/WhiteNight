# 首版发布验收清单

构建计划第 18 节逐项核对。`[ ]` 表示待用户/长时运行确认。

- [x] WebUI 稳定聊天、识图、处理约定文档并恢复历史会话（阶段 2/3/6 实测）
- [x] 正确记住、展示、修改和删除用户偏好与情景记忆（阶段 4）
- [x] 搜索结果包含来源；网页内容不能触发未授权工具操作（阶段 3）
- [x] 简单任务、Hermes 任务和 Codex 任务按规则正确路由（阶段 5 黄金集）
- [ ] Hermes/Codex 任务进度可见、可中止、可恢复或明确失败
      （进度事件已实现；Hermes 真实任务待用户登录 Provider，Codex 真实任务待配额确认）
- [x] 所有入口共享同一身份、会话、记忆、任务和权限记录（阶段 2/4/5/8）
- [x] QQ 只有所有者账号能触发工具和处理审批（阶段 8 契约测试）
- [x] 主动 QQ 消息服从频率、静默时段和暂停状态（阶段 7；真实 QQ 发送待 NapCat）
- [x] 删除单文件默认进废纸篓；批量删除无法由 Agent 执行（阶段 3 红队）
- [x] WebUI 和服务仅本机监听；凭据不以明文写入日志或配置（ADR-0002 + 脱敏）
- [ ] 重启、睡眠唤醒和网络中断不重复回复/丢失任务/串记忆
      （重启恢复实测；睡眠唤醒与长时网络中断待 72h 测试）
- [ ] 72 小时持续运行无阻断性故障和明显内存泄漏（脚本就绪，待执行）
- [ ] 无常驻人格 prompt 时最终 LoRA 模型通过用户人格盲测（阶段 9 待 GPU/用户）

## 发布前必做

1. `./scripts/check.sh` + `uv run mypy src/whitenight` 全绿。
2. 备份 → 迁移演练 → 恢复演练一次（真实数据目录）。
3. `uv run scripts/run_72h.py --hours 72`。
4. 用户完成 LoRA 盲测并选择默认模型；未完成前继续用 SOUL.md。
5. GitHub push（先 `gh auth refresh -s workflow`）。
