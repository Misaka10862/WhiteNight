[project]
name = "whitenight"
version = "0.1.0"
description = "WhiteNight: a local-first personal AI agent"
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
authors = [{ name = "Misaka10862" }]
keywords = ["agent", "local-first", "companion", "macos"]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Environment :: MacOS X",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
]
dependencies = [
    "alembic>=1.16,<2",
    "beautifulsoup4>=4.13,<5",
    "cryptography>=45,<47",
    "fastapi>=0.116,<1",
    "httpx>=0.28,<1",
    "openpyxl>=3.1,<4",
    "pillow==12.3.0",
    "pydantic>=2.11,<3",
    "pydantic-settings>=2.10,<3",
    "pymupdf>=1.26,<2",
    "python-docx>=1.2,<2",
    "python-pptx>=1.0,<2",
    "pyyaml>=6.0,<7",
    "regex>=2025.7,<2027",
    "sqlalchemy>=2.0.41,<3",
    "tokenizers>=0.21,<1",
    "uvicorn[standard]>=0.35,<1",
    "websockets==17.0.1",
]

[project.scripts]
whitenight = "whitenight.__main__:main"

[dependency-groups]
dev = [
    "mypy>=1.16,<2",
    "pytest>=8.4,<9",
    "pytest-cov>=6.2,<8",
    "ruff>=0.12,<1",
    "types-pyyaml>=6.0",
]

# SQLCipher driver is intentionally optional: only needed for the encrypted
# production database. The development/test stack uses plain SQLite.
[project.optional-dependencies]
sqlcipher = ["sqlcipher3>=0.6.2,<1"]
ocr = ["ocrmac>=1.0.1,<2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/whitenight"]

[tool.uv]
package = true
default-groups = ["dev"]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "SIM", # flake8-simplify
    "RUF", # ruff-specific
]
ignore = [
    "E501",
    "RUF001", # Ambiguous full-width punctuation in Chinese product text
    "RUF002", # Ambiguous full-width punctuation in Chinese product text
    "RUF003", # Ambiguous full-width punctuation in Chinese product text
]

[tool.ruff.lint.isort]
known-first-party = ["whitenight"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["whitenight"]
exclude = ["tests"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
pythonpath = ["src"]
